spring:
  jpa:
    show-sql: true
  kafka:
    consumer:
      auto-offset-reset: earliest
      properties:
        spring.json.trusted.packages: "com.spendmanagement.kafka.event"

logging:
  level:
    com.spendmanagement: DEBUG
    org.testcontainers: INFO
